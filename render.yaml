services:
  - type: web
    name: mta-subway-arrivals
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: bash start.sh
    envVars:
      - key: MTA_API_KEY
        sync: false
    healthCheckPath: /health
    autoDeploy: true
